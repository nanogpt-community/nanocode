{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["CI", "NANOGPT_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "NANOGPT_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "opencode#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@nanogpt/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
